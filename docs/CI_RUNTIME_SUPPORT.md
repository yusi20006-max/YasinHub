# CI Runtime Support

YasinHub declares Python >=3.9 and its CI matrix covers Python 3.9 through 3.13.

The matrix is intentionally aligned with the package's declared runtime contract and does not use compatibility bypasses.
